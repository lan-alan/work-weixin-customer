# 应用服务WEB端口
server:
  port: 8080
  servlet:
    context-path: /wwc

# 应用名称
spring:
  application:
    name: work-weixin-customer
  # redis 配置
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 3000
    jedis:
      pool:
        max-active: 1024
        max-wait: 10000
        max-idle: 200
        min-idle: 0
  # DataSource Config
  datasource:
    # MySQL 8.0+版本 com.mysql.cj.jdbc.Driver；5.0+ 版本 com.mysql.jdbc.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/lmjg?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: alan199402

mybatis-plus:
  configuration:
    # 打印SQL语句
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto   #设置主键自增
      # not_empty、not_null、default、ignored、never
      insert-strategy: not_empty	#插入策略
      update-strategy: not_empty	#更新策略

# log 日志配置 日志级别从低到高分为TRACE < DEBUG < INFO < WARN < ERROR < FATAL，如果设置为WARN，则低于WARN的信息都不会输出
logging:
  level:
    root: info
  file:
    path: /Users/alan.lan/Alan/Log/wwc/logs

wx-config:
  # 企业ID
  corpid: ww3aa88cdfa784f2f9
  # 微信客服secret
  corpsecret: A3nLuF1MsL5aILQnO8KiQUmZUh8ywMseegR35p135_0



